[pytest]
addopts =
    --html=reports/report.html
    --self-contained-html
    --alluredir=reports/allure-results
    --tb=short
    -v
    -p no:playwright
testpaths = tests
markers =
    smoke: Smoke tests - critical path validation
    regression: Full regression tests
    booking: End-to-end booking flow tests
